(function (window) {
    "use strict";

    window.Elevated = window.Elevated || {};
    window.Elevated.state = {
        createBase() {
            return {
                clues: [],
                clueMapItems: [],
            };
        },
    };
})(window);
